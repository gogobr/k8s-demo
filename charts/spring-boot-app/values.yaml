replicaCount: 1

image:
  repository: k8s-demo
  tag: v6
  pullPolicy: IfNotPresent

appConfig:
  greeting: "Hello from Helm Values!"

service:
  type: LoadBalancer

resources:
  requests:
  cpu: "1000m"
  memory: "1024Mi"
  limits:
    cpu: "1000m"
    memory: "1024Mi"