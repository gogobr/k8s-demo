{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "spring-boot-app.fullname" . }}
  labels:
    {{- include "my-chart.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                # 🔥【核心修改 1】优先读取 paths 下配置的 serviceName，如果没有，才回退到 Release.Name
                name: {{ .serviceName | default $.Release.Name }}
                port:
                  # 🔥【核心修改 2】优先读取 paths 下配置的 servicePort，如果没有，才回退到全局端口
                  number: {{ .servicePort | default $.Values.service.port | default 80 }}
          {{- end }}
    {{- end }}
{{- end }}